# 2026-03-22 작업 로그

## 작업 내용

### 1. DB 스키마 마이그레이션 (`supabase/migrations/002_product_fields.sql`)

`products` 테이블에 판매자 상품 등록에 필요한 컬럼 추가:

| 분류 | 추가 컬럼 |
|---|---|
| 기본 정보 | product_code, variety, color[], characteristics, image_urls[] |
| 상품 특성 | flower_size, blooming_season, freshness_grade, has_thorns, has_fragrance |
| 판매 조건 | min_order_quantity, sale_start_date, sale_end_date, bulk_discount_conditions(JSONB) |
| 배송 정보 | origin, deliverable_regions[], shipping_methods[], shipping_days_required, shipping_fee, order_cutoff_time, cold_packaging |
| 출하 일정 | available_shipping_days[], expected_arrival_days[], harvest_date, sale_season |
| 추천/유의사항 | recommended_buyer_types[], notes |

`stores` 테이블에 판매자 등급 정보 추가:
- `seller_grade` (certified / general)
- `origin_certification` (gap / organic / none)

> Supabase SQL Editor에서 직접 실행 필요

---

### 2. TypeScript 타입 업데이트 (`src/types/index.ts`)

- `Product` 인터페이스에 신규 필드 전체 반영
- `Store` 인터페이스에 `seller_grade`, `origin_certification` 추가
- 신규 타입 추가: `FlowerSize`, `FreshnessGrade`, `SaleSeason`, `SellerGrade`, `OriginCertification`, `BulkDiscountCondition`

---

### 3. 상품 등록 폼 (`app/seller/product-form.tsx`) 신규 생성

- 4단계 스텝 폼 구성 (기본정보+상품특성 / 판매조건 / 배송정보 / 일정&추천)
- 카테고리 선택 시 해당 품종 칩 자동 표시 (categories.md 기반)
- 직접 입력 병행 가능
- `_layout.tsx`에서 탭 숨김 처리

---

### 4. 상품 목록 (`app/seller/products.tsx`) 수정

- 기존 인라인 모달 제거
- `+ 추가` 버튼 → `router.push('/seller/product-form')`으로 변경
- 상품카드에 품종, 산지 표시 추가

---

### 5. 가게 관리 플로우 분리

**신규 파일: `app/seller/store-form.tsx`**
- 가게 등록 / 수정 폼 분리
- `storeId` 파라미터로 신규/수정 모드 구분
- 저장 후 `router.back()`

**수정 파일: `app/seller/store.tsx`**
- 가게 목록 화면으로 전면 재작성
- `useFocusEffect`로 폼에서 돌아올 때 목록 자동 새로고침
- 가게 없을 시 빈 화면 + "첫 가게 등록하기" 버튼 노출

**서비스 추가: `src/services/stores.ts`**
- `getMyStores(sellerId)` 메서드 추가 (배열 반환)

---

### 6. 하단 탭 구성 변경 (`app/seller/_layout.tsx`)

변경 전: 가게 / 상품 / 주문 / 커뮤니티 / 설정

변경 후:

| 탭 | 파일 |
|---|---|
| 내 가게 | store.tsx |
| 판매상품 | products.tsx |
| 주문 | orders.tsx |
| 내 정보 | setting.tsx |

숨김 처리: home, product-form, store-form, community

---

### 7. docs 업데이트

- `docs/seller.md` 전면 업데이트 (가게 관리, 상품 관리, 탭 구성 반영)
